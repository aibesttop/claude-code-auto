# Phase 0: 预算管理系统 - 使用指南

> 🎉 **完成日期**: 2025-11-21
> 📦 **版本**: V3.1-alpha
> 🎯 **状态**: ✅ 已完成并测试

---

## 📋 目录

1. [功能概述](#功能概述)
2. [快速开始](#快速开始)
3. [配置说明](#配置说明)
4. [使用方法](#使用方法)
5. [监控工具](#监控工具)
6. [测试运行](#测试运行)
7. [故障排除](#故障排除)

---

## 功能概述

### ✨ 核心特性

| 功能 | 说明 | 状态 |
|------|------|------|
| **多层级预算** | 日/周/月预算控制 | ✅ |
| **Agent配额** | 按Agent类型分配预算 | ✅ |
| **智能降级** | 自动切换到便宜模型 | ✅ |
| **实时监控** | 预算状态可视化 | ✅ |
| **历史追踪** | 自动保存使用数据 | ✅ |
| **告警系统** | 80%/95% 阈值告警 | ✅ |

### 💰 成本节省效果

- **自动降级**: Sonnet ($3/M) → Haiku ($0.25/M) = **节省 87%**
- **预算防护**: 防止意外成本失控
- **Agent配额**: 单个Agent不会耗尽预算

---

## 快速开始

### 1️⃣ 安装依赖

```bash
pip install -r requirements.txt
```

新增依赖:
- `rich>=13.7.0` - 终端美化
- `pytest>=7.4.0` - 测试框架
- `pytest-asyncio>=0.21.0` - 异步测试

### 2️⃣ 配置预算

编辑 `config.yaml`:

```yaml
budget:
  daily_budget: 10.0        # 每日预算（美元）
  weekly_budget: 70.0       # 每周预算
  monthly_budget: 300.0     # 每月预算
  enable_auto_fallback: true # 启用自动降级
  storage_dir: "logs/budget"
  agent_ratios:             # Agent预算分配
    planner: 0.1            # 10% - 规划
    executor: 0.6           # 60% - 执行
    researcher: 0.3         # 30% - 研究
```

### 3️⃣ 运行系统

```bash
python main_v3.py
```

预算管理器会自动集成到主流程中：
- ✅ 启动时初始化
- ✅ 执行前检查预算
- ✅ 预算不足时自动降级
- ✅ 执行后记录成本
- ✅ 结束时生成报告

---

## 配置说明

### 预算配置 (config.yaml)

```yaml
budget:
  # 预算限制
  daily_budget: 10.0        # 必填，最小 0.1
  weekly_budget: 70.0       # 可选，默认 daily_budget * 7
  monthly_budget: 300.0     # 可选，默认 daily_budget * 30

  # 降级策略
  enable_auto_fallback: true  # 启用/禁用自动降级

  # 存储
  storage_dir: "logs/budget"  # 使用数据存储目录

  # Agent预算分配（总和应为 1.0）
  agent_ratios:
    planner: 0.1      # 规划 Agent（轻量）
    executor: 0.6     # 执行 Agent（主力）
    researcher: 0.3   # 研究 Agent（中等）
```

### 环境变量覆盖

```bash
# 覆盖日预算
export WORKFLOW_DAILY_BUDGET=20.0

# 禁用自动降级
export WORKFLOW_AUTO_FALLBACK=false
```

---

## 使用方法

### 编程式使用

#### 基础用法

```python
from core.budget_manager import BudgetManager

# 创建管理器
manager = BudgetManager(
    daily_budget=10.0,
    enable_auto_fallback=True
)

# 检查预算
result = await manager.check_budget(
    agent_type="executor",
    operation="llm_call",
    estimated_cost=0.05,
    model="claude-3-5-sonnet-20241022"
)

if result.allowed:
    # 执行操作
    actual_cost = perform_llm_call()

    # 记录使用
    manager.record_usage(
        agent_type="executor",
        operation="llm_call",
        actual_cost=actual_cost,
        model=result.recommended_model or "claude-3-5-sonnet-20241022",
        fallback_applied=(result.recommended_model is not None)
    )
else:
    print(f"操作被阻止: {result.warning_message}")
```

#### 成本估算

```python
# 从 token 数估算
cost = manager.estimate_cost(
    input_tokens=100_000,
    output_tokens=50_000,
    model="claude-3-5-sonnet-20241022"
)

# 从文本长度估算
cost = manager.estimate_cost_from_text(
    input_text="你的输入文本",
    output_text="预期输出文本",
    model="claude-3-5-sonnet-20241022"
)
```

#### 生成报告

```python
# 生成报告
report = manager.generate_report()
print(f"总成本: ${report['total_cost']:.4f}")
print(f"使用率: {report['usage_percentage']:.2f}%")

# 打印美化报告
manager.print_report()

# 获取实时状态
status = manager.get_budget_status()
print(f"状态: {status['status']}")
print(f"剩余预算: ${status['remaining_budget']:.4f}")
```

---

## 监控工具

### 预算监控工具 (budget_monitor.py)

独立的命令行工具，用于实时监控预算状态。

#### 安装 Rich 库

```bash
pip install rich>=13.7.0
```

#### 命令

##### 1. 查看当前状态

```bash
python budget_monitor.py status
```

输出:
```
┌─────────────────────────────────┐
│      💰 Budget Status           │
│                                 │
│ Status: HEALTHY                 │
│ Budget: $2.45 / $10.00         │
│ Usage: 24.5%                   │
│ Remaining: $7.55               │
│                                 │
│ ████████░░░░░░░░░░░░░░░░░░░░ 24.5% │
└─────────────────────────────────┘
```

##### 2. 生成详细报告

```bash
python budget_monitor.py report
```

输出完整报告，包括:
- 总成本和使用率
- Agent 分解
- 操作分解
- 降级统计

##### 3. 实时监控（每5秒刷新）

```bash
python budget_monitor.py watch
```

按 `Ctrl+C` 退出监控。

##### 4. 查看历史数据

```bash
# 查看最近 7 天
python budget_monitor.py history 7

# 查看最近 30 天
python budget_monitor.py history 30
```

##### 5. 重置日预算

```bash
python budget_monitor.py reset
```

⚠️ **警告**: 此操作会清空今天的使用记录！

---

## 测试运行

### 运行单元测试

```bash
# 运行所有测试
pytest test_budget_manager.py -v

# 运行特定测试类
pytest test_budget_manager.py::TestBudgetCheck -v

# 查看覆盖率（需要 pytest-cov）
pip install pytest-cov
pytest test_budget_manager.py --cov=core.budget_manager --cov-report=html
```

### 测试用例概览

| 测试类 | 测试数量 | 覆盖内容 |
|--------|---------|---------|
| `TestBudgetInitialization` | 3 | 初始化和配置 |
| `TestCostEstimation` | 3 | 成本估算 |
| `TestBudgetCheck` | 4 | 预算检查 |
| `TestFallbackStrategies` | 3 | 降级策略 |
| `TestUsageRecording` | 3 | 使用记录 |
| `TestReporting` | 3 | 报告生成 |
| `TestBudgetStatus` | 4 | 状态监控 |
| `TestPersistence` | 2 | 持久化 |
| `TestEdgeCases` | 3 | 边界情况 |

**总计**: 28+ 测试用例

---

## 故障排除

### 问题 1: 导入错误

**错误**:
```
ModuleNotFoundError: No module named 'pydantic'
```

**解决**:
```bash
pip install -r requirements.txt
```

---

### 问题 2: 预算数据未保存

**原因**: 使用记录少于 10 条时不会自动保存。

**解决**:
```python
# 手动保存
manager._save_usage_history()
```

或者等待 10 条记录后自动保存。

---

### 问题 3: 历史数据加载失败

**原因**: 存储目录不存在或文件损坏。

**解决**:
```bash
# 检查存储目录
ls -la logs/budget/

# 手动创建目录
mkdir -p logs/budget

# 删除损坏的文件
rm logs/budget/budget_usage_*.json
```

---

### 问题 4: 预算检查总是阻止

**原因**:
1. 预算已耗尽
2. 禁用了自动降级 (`enable_auto_fallback: false`)

**解决**:
```yaml
# config.yaml
budget:
  enable_auto_fallback: true  # 启用自动降级
```

或者重置预算:
```bash
python budget_monitor.py reset
```

---

### 问题 5: 监控工具显示空数据

**原因**: 今天没有使用记录。

**解决**: 运行一次 `main_v3.py` 生成使用数据。

---

## 📊 架构概览

```
┌─────────────────────────────────────────────┐
│           main_v3.py (主流程)                │
│                                             │
│  ┌─────────────────────────────────┐       │
│  │  1. 初始化 BudgetManager         │       │
│  └─────────────────────────────────┘       │
│                  ↓                          │
│  ┌─────────────────────────────────┐       │
│  │  2. 执行前检查预算               │       │
│  │     - check_budget()            │       │
│  │     - 获取降级建议               │       │
│  └─────────────────────────────────┘       │
│                  ↓                          │
│  ┌─────────────────────────────────┐       │
│  │  3. 应用降级策略                 │       │
│  │     - 切换到 Haiku              │       │
│  │     - 或仅使用缓存               │       │
│  └─────────────────────────────────┘       │
│                  ↓                          │
│  ┌─────────────────────────────────┐       │
│  │  4. 执行操作                     │       │
│  │     - executor.execute_task()   │       │
│  └─────────────────────────────────┘       │
│                  ↓                          │
│  ┌─────────────────────────────────┐       │
│  │  5. 记录实际成本                 │       │
│  │     - record_usage()            │       │
│  └─────────────────────────────────┘       │
│                  ↓                          │
│  ┌─────────────────────────────────┐       │
│  │  6. 生成报告                     │       │
│  │     - generate_report()         │       │
│  │     - get_budget_status()       │       │
│  └─────────────────────────────────┘       │
└─────────────────────────────────────────────┘

              ↕ (持久化)

┌─────────────────────────────────────────────┐
│      logs/budget/                           │
│        budget_usage_2025-11-21.json         │
└─────────────────────────────────────────────┘

              ↓ (监控)

┌─────────────────────────────────────────────┐
│      budget_monitor.py                      │
│        - status / report / watch            │
└─────────────────────────────────────────────┘
```

---

## 🎯 关键指标

### 预算阈值

| 使用率 | 状态 | 行为 |
|--------|------|------|
| 0-79% | `healthy` | 正常运行 |
| 80-94% | `caution` | 显示警告 |
| 95-99% | `warning` | 自动降级 |
| 100%+ | `critical` | 阻止操作 |

### 模型成本对比

| 模型 | 输入 ($/M tokens) | 输出 ($/M tokens) | 相对成本 |
|------|------------------|------------------|---------|
| Opus | $15.00 | $75.00 | 6x |
| Sonnet | $3.00 | $15.00 | 1x (基准) |
| Haiku | $0.25 | $1.25 | 0.08x |

**降级效果**: Sonnet → Haiku = **节省 87%**

---

## 📝 变更日志

### V3.1-alpha (2025-11-21)

#### 新增
- ✨ 完整的预算管理系统
- ✨ 多层级预算控制
- ✨ 智能降级策略
- ✨ 独立监控工具
- ✨ 完整单元测试

#### 文件
- `core/budget_manager.py` (510 行)
- `test_budget_manager.py` (650 行)
- `budget_monitor.py` (380 行)
- `config.py` (新增 BudgetConfig)
- `config.yaml` (新增 budget 配置)
- `main_v3.py` (集成预算管理)
- `requirements.txt` (新增依赖)

---

## 🔜 下一步

### Phase 1: 核心能力增强 (2 周)

1. **Persona 优化** (3 天)
   - 使用 LLM 元提示压缩 Persona
   - 节省 20%+ token

2. **Researcher NLI 验证** (4 天)
   - 集成 CrossEncoder 重排序
   - NLI 幻觉检测

3. **Tool Composer** (5 天)
   - 安全的函数组合器
   - 20+ 预定义原语

### Phase 2: 多 Agent 协作 (2 周)

- Sub-Agent Orchestrator
- 任务依赖管理
- 并行执行

---

## 📚 参考资源

- [V3 生产级完善计划](V3_PRODUCTION_PLAN.md)
- [Budget Manager API 文档](core/budget_manager.py)
- [测试文档](test_budget_manager.py)

---

## 👥 贡献者

- **开发**: Claude + Human
- **测试**: 自动化测试套件
- **文档**: 本 README

---

## 📄 许可证

MIT License

---

**🎉 恭喜！Phase 0 预算管理系统已完成！**

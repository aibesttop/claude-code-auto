# Tool Mappings
# Maps mission types to required tools and MCP servers

mappings:
  # Market Research missions
  market_research:
    required_tools:
      - web_search
      - deep_research
      - write_file
    optional_tools:
      - web_fetch
      - quick_research
    mcp_servers:
      - brave_search
      - filesystem
      - sequential-thinking  # Enhanced reasoning for complex analysis

  # Documentation writing missions
  documentation:
    required_tools:
      - write_file
      - read_file
      - list_dir
    optional_tools:
      - web_search
    mcp_servers:
      - filesystem

  # Code generation missions
  code_generation:
    required_tools:
      - write_file
      - read_file
    optional_tools:
      - git_commit
    mcp_servers:
      - filesystem
      - git
      - serena  # Enhanced code analysis and project context

  # Architecture design missions
  architecture_design:
    required_tools:
      - write_file
      - read_file
    optional_tools:
      - web_search
    mcp_servers:
      - filesystem
      - sequential-thinking  # Step-by-step reasoning for complex architecture

  # SEO strategy missions
  seo_strategy:
    required_tools:
      - web_search
      - write_file
    optional_tools:
      - web_fetch
    mcp_servers:
      - brave_search
      - filesystem

  # Creative exploration missions
  creative_exploration:
    required_tools:
      - web_search
      - write_file
    optional_tools:
      - deep_research
    mcp_servers:
      - brave_search
      - filesystem

  # Database design missions
  database_design:
    required_tools:
      - write_file
    optional_tools:
      - query_database
    mcp_servers:
      - filesystem
      - postgres

  # Web automation missions (NEW)
  web_automation:
    required_tools:
      - web_search
      - write_file
    optional_tools:
      - run_command
    mcp_servers:
      - playwright
      - browsermcp
      - filesystem

  # Web scraping missions (NEW)
  web_scraping:
    required_tools:
      - web_search
      - write_file
    optional_tools:
      - run_command
    mcp_servers:
      - playwright
      - browsermcp
      - filesystem

  # Code analysis missions (NEW)
  code_analysis:
    required_tools:
      - read_file
      - write_file
    optional_tools:
      - run_command
    mcp_servers:
      - serena
      - filesystem
      - git

  # Complex problem solving missions (NEW)
  complex_problem_solving:
    required_tools:
      - write_file
      - read_file
    optional_tools:
      - web_search
    mcp_servers:
      - sequential-thinking
      - filesystem

  # Long-term memory missions (NEW)
  knowledge_management:
    required_tools:
      - write_file
      - read_file
    optional_tools:
      - web_search
    mcp_servers:
      - context7  # Upstash for semantic search and long-term memory
      - filesystem

  # General missions (fallback)
  general:
    required_tools:
      - write_file
      - read_file
    optional_tools:
      - web_search
    mcp_servers:
      - filesystem
